This repos contains two major components:
* Cucumber features for acceptance testing
* Within Vagrant modules and manifests are located to startup various versions of ownCloud on various systems


CUCUMER FEATURES:
-----------------

* Installation:
  Please install ruby and rubygems
  We support bundler:
  - gem install bundler
  - bundle install

* To run the cucumber features:
  - bundle exec cucumber HOST=localhost features

